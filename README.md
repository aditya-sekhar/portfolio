# Aditya Sekhar — Portfolio

Source for [adityasekhar.dev-style portfolio](https://aditya-sekhar.github.io/portfolio) — a single static page (`index.html` + `style.css`, no build step) covering current work as an AI Architect at Comcast, prior platform/security architecture work, and side projects.

Deployed via GitHub Pages using the Actions workflow in `.github/workflows/deploy-pages.yml`. To update: edit `index.html` / `style.css` and push to `main`.
